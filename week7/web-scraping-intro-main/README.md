# Web Scraping Intro

A Python project demonstrating basic web scraping using `requests` and `BeautifulSoup4`.

## Requirements
* Python 3.x
* dependencies in `requirements.txt`

## Execution
```cmd
python main.py